from setuptools import setup

setup(name='period_aggr',
      version='0.0.1',
      description='testcode',
      author='test company ltd',
      author_email='test@test.com',
      packages=['job'],
      zip_safe=False)